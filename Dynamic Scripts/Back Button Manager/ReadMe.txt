user manual :
add the BackButton Controller to any panel that can be closed using the BackButton
-----------------------------------------------------------------------------------
you can also use new input system
-----------------------------------------------------------------------------------
reminder : if multiple panels are active at the same time the one with higher order is closed first
-----------------------------------------------------------------------------------
in case of errors => import new input system or comment related lines (there is no problem)